
<div class="top-spacing text-center">
  <h1><%= current_user.nickname %>さん、ようこそNinjavaScriptへ！</h1>
  <p>今日も日本語学習を頑張りましょう！</p>
  <% if false %><%# <% if current_user.mentor.present? %><%# を一時的に無効化 %>
    <p><%= current_user.nickname %>さんのメンターは<%= current_user.mentor.nickname %>さんです</p>
  <% else %>
    <p><%= current_user.nickname %>さんのメンターはまだいません。登録をおねがいします。</p>
  <% end %>
</div>


<ul>
  <li>
    <%= link_to '単語登録 -Add Word -', new_user_word_path(current_user) %>
    <span class="language">たんごとうろく</span>
  </li>
  <li>
    <%= link_to "#{current_user.nickname}さんの単語帳 -Your Wordbook-", user_words_path(current_user) %>
    <span class="language"><%= "#{current_user.nickname}さんのたんごちょう" %></span>
  </li>
    <% if current_user.words.count >= 4 %>  <!-- 単語が4つ以上存在する場合のみ表示 -->
    <% @word = current_user.words.sample %> <!-- ランダムで一つの単語を選ぶ -->
    <li>
      <%= link_to '単語テスト -Word Quiz-', user_word_quizzes_path(current_user, @word.id) %>
      <span class="language">たんごてすと</span>
    </li>
  <% else %> <!-- 単語が存在しない場合 -->
    <li>
      <p>テストを開始するには、まず単語を登録してください。</p>
    </li>
  <% end %>
  <li>
    <%= link_to '日報 -Progress Check-', '#' %>
    <span class="language">にっぽう</span>
  </li>
  <li>
    <%= link_to 'コメント入力 -Comment Input- (メンターのみ)', '#' %>
    <span class="language">こめんとにゅうりょく</span>
  </li>
  <li>
    <%= link_to 'メンター登録 -Mentor Registration-', '#' %>
    <span class="language">めんたーとうろく</span>
  </li>
</ul>







